const translations = {
  en: {
    "nav.about": "About",
    "nav.publications": "Publications",
    "nav.openSource": "Open source",
    "nav.contact": "Contact",
    "hero.statement": "I build generative systems that connect motion, language, audio, and physical intelligence.",
    "hero.explore": "Explore research",
    "hero.email": "Email me",
    "hero.current": "Current position",
    "hero.role": "Researcher, Tencent Hunyuan3D Team",
    "hero.workFocus": "3D Motion Generation",
    "hero.educationLabel": "Education",
    "hero.education": "Ph.D. candidate, Zhejiang University",
    "hero.caption": "Selected research demos",
    "hero.pauseReel": "Pause research reel",
    "hero.playReel": "Play research reel",
    "about.eyebrow": "About",
    "about.title": "I build multimodal systems for understanding, generating, and executing human motion.",
    "about.body": "My research focuses on 3D human motion generation and multimodal generative models, including motion-language learning, human motion understanding, and text- or audio-conditioned synthesis for single-person and multi-person scenarios.",
    "about.educationTitle": "Education",
    "about.phd": "Ph.D. Candidate in Computer Science",
    "about.zju": "Zhejiang University · State Key Lab of CAD&CG",
    "about.advisors": "Advised by Prof. Changqing Zou and Prof. Weidong Geng",
    "about.master": "M.Eng. in Software Engineering",
    "about.zjuShort": "Zhejiang University",
    "about.bachelor": "B.Eng. in Software Engineering",
    "about.zjsu": "Zhejiang Gongshang University",
    "about.experienceTitle": "Experience",
    "about.tencentRole": "Algorithm Researcher",
    "about.tencentOrg": "Tencent · Hunyuan3D Team",
    "about.tencentFocus": "Working on 3D Motion Generation.",
    "about.intern": "Algorithm Research Intern",
    "about.zjlab": "Zhejiang Lab",
    "research.motionGeneration": "3D Human Motion Generation",
    "research.motionLanguage": "Motion-Language Learning",
    "research.multimodalModels": "Multimodal Generative Models",
    "research.motionUnderstanding": "Human Motion Understanding",
    "common.present": "Present",
    "common.firstAuthor": "First author",
    "common.thirdAuthor": "Third author",
    "common.coAuthor": "Co-author",
    "common.figure": "Figure",
    "common.demo": "Demo",
    "common.noDemo": "No public demo",
    "common.codeUnavailable": "Code unavailable",
    "common.openMenu": "Open menu",
    "common.closeMenu": "Close menu",
    "common.useDarkTheme": "Use dark theme",
    "common.useLightTheme": "Use light theme",
    "common.filterPublications": "Filter publications",
    "common.primaryActions": "Primary actions",
    "common.loadingVideo": "Loading video",
    "common.videoError": "The video could not be loaded. Retry here or open it directly.",
    "common.retry": "Retry",
    "common.openVideo": "Open video",
    "news.title": "Recent news",
    "news.gentrack": "GenTrack was released as an arXiv preprint.",
    "news.prism": "PRISM was released as an arXiv preprint.",
    "news.versatile": "VersatileMotion was accepted to ECCV 2026.",
    "news.enchant": "EnchantDance received the ChinaMM 2026 Best Paper Award and was accepted by IEEE TMM.",
    "news.synclip": "SyncLipMAE was released as an arXiv preprint.",
    "news.epic": "EPIC was accepted to ICME 2025 as an Oral paper.",
    "news.mcm": "MCM was accepted to IJCAI 2024.",
    "publications.eyebrow": "Research",
    "publications.title": "Publications",
    "publications.all": "All",
    "publications.motion": "Motion",
    "publications.multimodal": "Multimodal",
    "publications.countOne": "1 publication",
    "publications.countMany": "{count} publications",
    "publications.viewAll": "Complete record on Google Scholar",
    "papers.gentrack": "An online generator–tracker framework that uses execution feedback to produce robot-native motions and broaden zero-shot humanoid tracking coverage.",
    "papers.prism": "A continuous kinematic-unit latent manifold that preserves articulated structure and gives one SMPL-native flow model text-to-motion, pose-conditioned continuation, sequential rollout, and narrative motion composition capabilities.",
    "papers.versatile": "A motion-language framework that unifies synthesis and comprehension across single-person and multi-person tasks.",
    "papers.synclip": "Contrastive masked pretraining for a shared talking-face representation across synchronization, facial understanding, visual speech recognition, and dubbing.",
    "papers.enchant": "Music-driven 3D dance generation with a large dance dataset, a dance VAE, and a diffusion-based generation model.",
    "papers.enchantAward": "ChinaMM 2026 · Best Paper",
    "papers.epic": "An efficient prompt-interaction strategy for text-image classification that tunes about one percent of the foundation model parameters.",
    "papers.mcm": "A unified motion synthesis framework for combining text, music, and other control conditions through a shared control branch.",
    "openSource.eyebrow": "Open source",
    "openSource.title": "Research infrastructure",
    "openSource.body": "Reusable foundations for multimodal research, scalable model training, open datasets, and agent-assisted research workflows.",
    "openSource.motius": "A modular framework for motion training, evaluation, inference, representation conversion, and reproducible leaderboards.",
    "openSource.hftrainer": "Config-driven training for Hugging Face-native models with Accelerate, Transformers, Diffusers, and PEFT.",
    "openSource.motionHub": "A curated human-motion collection spanning text, music, speech, and two-person interaction.",
    "openSource.researchSkills": "Composable, evidence-aware agent workflows spanning literature discovery, idea refinement, audited experiments, manuscript development, scientific figures, review, and rebuttal.",
    "contact.eyebrow": "Contact",
    "contact.body": "I am glad to discuss generative AI, multimodal learning, embodied intelligence, and open research collaboration.",
    "footer.note": "Generative AI · multimodal learning · embodied intelligence"
  },
  zh: {
    "nav.about": "关于",
    "nav.publications": "论文",
    "nav.openSource": "开源",
    "nav.contact": "联系",
    "hero.statement": "研究连接动作、语言、音频与物理智能的生成式系统。",
    "hero.explore": "浏览研究工作",
    "hero.email": "邮件联系",
    "hero.current": "目前职位",
    "hero.role": "腾讯 Hunyuan3D 团队算法研究员",
    "hero.workFocus": "3D 动作生成",
    "hero.educationLabel": "教育背景",
    "hero.education": "浙江大学计算机博士生",
    "hero.caption": "精选研究演示",
    "hero.pauseReel": "暂停研究视频合集",
    "hero.playReel": "播放研究视频合集",
    "about.eyebrow": "关于",
    "about.title": "研究理解、生成与执行人体动作的多模态系统。",
    "about.body": "主要研究 3D 人体动作生成与多模态生成模型，包括动作语言学习、人体动作理解，以及面向单人和多人场景的文本或音频驱动动作合成。",
    "about.educationTitle": "教育经历",
    "about.phd": "计算机科学与技术博士生",
    "about.zju": "浙江大学 · CAD&CG 国家重点实验室",
    "about.advisors": "博士期间受邹长青教授、耿卫东教授指导",
    "about.master": "软件工程硕士",
    "about.zjuShort": "浙江大学",
    "about.bachelor": "软件工程学士",
    "about.zjsu": "浙江工商大学",
    "about.experienceTitle": "工作经历",
    "about.tencentRole": "算法研究员",
    "about.tencentOrg": "腾讯 · Hunyuan3D 团队",
    "about.tencentFocus": "从事 3D 动作生成研究。",
    "about.intern": "算法研究实习生",
    "about.zjlab": "之江实验室",
    "research.motionGeneration": "3D 人体动作生成",
    "research.motionLanguage": "动作语言学习",
    "research.multimodalModels": "多模态生成模型",
    "research.motionUnderstanding": "人体动作理解",
    "common.present": "至今",
    "common.firstAuthor": "第一作者",
    "common.thirdAuthor": "第三作者",
    "common.coAuthor": "共同作者",
    "common.figure": "方法图",
    "common.demo": "演示",
    "common.noDemo": "暂无公开视频",
    "common.codeUnavailable": "代码暂未公开",
    "common.openMenu": "打开菜单",
    "common.closeMenu": "关闭菜单",
    "common.useDarkTheme": "使用深色主题",
    "common.useLightTheme": "使用浅色主题",
    "common.filterPublications": "筛选论文",
    "common.primaryActions": "主要操作",
    "common.loadingVideo": "正在加载视频",
    "common.videoError": "视频加载失败。你可以重试或直接打开视频。",
    "common.retry": "重试",
    "common.openVideo": "打开视频",
    "news.title": "近期动态",
    "news.gentrack": "发布 GenTrack arXiv 预印本。",
    "news.prism": "发布 PRISM arXiv 预印本。",
    "news.versatile": "VersatileMotion 被 ECCV 2026 接收。",
    "news.enchant": "EnchantDance 获 ChinaMM 2026 最佳论文奖，并被 IEEE TMM 接收。",
    "news.synclip": "发布 SyncLipMAE arXiv 预印本。",
    "news.epic": "EPIC 被 ICME 2025 接收为 Oral 论文。",
    "news.mcm": "MCM 被 IJCAI 2024 接收。",
    "publications.eyebrow": "研究",
    "publications.title": "论文",
    "publications.all": "全部",
    "publications.motion": "动作",
    "publications.multimodal": "多模态",
    "publications.countOne": "1 篇论文",
    "publications.countMany": "{count} 篇论文",
    "publications.viewAll": "在 Google Scholar 查看完整论文记录",
    "papers.gentrack": "以在线生成器—跟踪器框架联结执行反馈、机器人原生动作生成与零样本人形机器人跟踪覆盖。",
    "papers.prism": "以连续的运动学单元隐空间保留人体拓扑与关节结构，使同一个 SMPL 原生流模型同时具备文本生成动作、姿态条件续写、序列展开与叙事动作组合能力。",
    "papers.versatile": "统一动作生成与理解的动作语言框架，覆盖单人和多人任务。",
    "papers.synclip": "面向唇音同步、人脸理解、视觉语音识别和配音任务的对比式掩码音视频预训练方法。",
    "papers.enchant": "结合大规模舞蹈数据集、Dance VAE 与扩散生成模型的音乐驱动 3D 舞蹈生成方法。",
    "papers.enchantAward": "ChinaMM 2026 · 最佳论文",
    "papers.epic": "高效的文本与图像提示交互方法，仅需微调约百分之一的基础模型参数。",
    "papers.mcm": "通过共享控制分支统一组合文本、音乐等多种条件的动作合成框架。",
    "openSource.eyebrow": "开源",
    "openSource.title": "研究基础设施",
    "openSource.body": "面向多模态研究、规模化模型训练、开放数据与 Agent 辅助科研工作流的可复用基础设施。",
    "openSource.motius": "模块化动作训练、评测与推理框架，覆盖表征转换、模型库和可复现排行榜。",
    "openSource.hftrainer": "面向 Hugging Face 模型的配置驱动训练框架，集成 Accelerate、Transformers、Diffusers 与 PEFT。",
    "openSource.motionHub": "覆盖文本、音乐、语音和双人交互的统一人体动作数据集。",
    "openSource.researchSkills": "可组合、证据可追溯的 Agent 科研工作流，覆盖文献发现、idea 打磨、可审计实验、论文写作、科研作图、评审与 rebuttal。",
    "contact.eyebrow": "联系",
    "contact.body": "欢迎讨论生成式 AI、多模态学习、具身智能与开放研究合作。",
    "footer.note": "生成式 AI · 多模态学习 · 具身智能"
  }
};

const root = document.documentElement;
const header = document.querySelector(".site-header");
const languageButton = document.querySelector(".language-toggle");
const themeButton = document.querySelector(".theme-toggle");
const menuButton = document.querySelector(".menu-toggle");
const mobileNav = document.querySelector(".mobile-nav");
const mainContent = document.querySelector("main");
const siteFooter = document.querySelector(".site-footer");
const filterButtons = [...document.querySelectorAll(".filter-button")];
const papers = [...document.querySelectorAll(".paper-item")];
const filterStatus = document.querySelector("[data-filter-status]");
const systemThemeQuery = window.matchMedia("(prefers-color-scheme: dark)");

function readPreference(key) {
  try {
    return window.localStorage.getItem(key);
  } catch {
    return null;
  }
}

function writePreference(key, value) {
  try {
    window.localStorage.setItem(key, value);
  } catch {
    // Theme and language still work for the current page when storage is unavailable.
  }
}

const storedLanguage = readPreference("language");
const storedTheme = readPreference("theme");
let language = storedLanguage === "zh" ? "zh" : "en";
let menuReturnFocus = null;

function text(key) {
  return translations[language][key] || key;
}

function criticalIcon(name) {
  const paths = {
    menu: '<path d="M4 6H20M4 12H20M4 18H20" />',
    x: '<path d="M18 6 6 18M6 6l12 12" />',
    moon: '<path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79Z" />',
    sun: '<circle cx="12" cy="12" r="4" /><path d="M12 2V4M12 20V22M4.93 4.93 6.34 6.34M17.66 17.66 19.07 19.07M2 12H4M20 12H22M4.93 19.07 6.34 17.66M17.66 6.34 19.07 4.93" />'
  };
  return `<svg viewBox="0 0 24 24" aria-hidden="true">${paths[name]}</svg>`;
}

function updateIcons() {
  if (window.lucide) window.lucide.createIcons();
}

function updateFilterStatus(count = papers.filter((paper) => !paper.hidden).length) {
  if (!filterStatus) return;
  const key = count === 1 ? "publications.countOne" : "publications.countMany";
  filterStatus.dataset.count = String(count);
  filterStatus.textContent = text(key).replace("{count}", String(count));
}

function syncMenuButton() {
  const isOpen = !mobileNav.hidden;
  const label = text(isOpen ? "common.closeMenu" : "common.openMenu");
  menuButton.innerHTML = criticalIcon(isOpen ? "x" : "menu");
  menuButton.setAttribute("aria-label", label);
  menuButton.setAttribute("title", label);
  menuButton.setAttribute("aria-expanded", String(isOpen));
}

function syncThemeButton() {
  const isDark = root.dataset.theme === "dark";
  const label = text(isDark ? "common.useLightTheme" : "common.useDarkTheme");
  themeButton.innerHTML = criticalIcon(isDark ? "sun" : "moon");
  themeButton.setAttribute("aria-label", label);
  themeButton.setAttribute("title", label);
}

function syncMediaLanguage() {
  document.querySelectorAll("[data-media-error-copy]").forEach((element) => {
    element.textContent = text("common.videoError");
  });
  document.querySelectorAll("[data-media-retry]").forEach((element) => {
    element.textContent = text("common.retry");
  });
  document.querySelectorAll("[data-media-open]").forEach((element) => {
    element.textContent = text("common.openVideo");
  });
}

function setLanguage(nextLanguage) {
  language = nextLanguage;
  root.lang = language === "zh" ? "zh-CN" : "en";
  document.querySelectorAll("[data-i18n]").forEach((element) => {
    const value = translations[language][element.dataset.i18n];
    if (value) element.textContent = value;
  });
  languageButton.querySelector("span").textContent = language === "en" ? "中" : "EN";
  const languageLabel = language === "en" ? "Switch to Chinese" : "切换到英文";
  languageButton.setAttribute("aria-label", languageLabel);
  languageButton.setAttribute("title", languageLabel);
  document.querySelector(".desktop-nav")?.setAttribute("aria-label", language === "en" ? "Primary navigation" : "主导航");
  mobileNav.setAttribute("aria-label", language === "en" ? "Mobile navigation" : "移动导航");
  document.querySelector(".filter-tabs")?.setAttribute("aria-label", text("common.filterPublications"));
  document.querySelector(".hero-actions")?.setAttribute("aria-label", text("common.primaryActions"));
  syncMenuButton();
  syncThemeButton();
  syncMediaLanguage();
  updateFilterStatus();
  writePreference("language", language);
  window.heroMontageController?.syncControl();
}

function setTheme(theme, { persist = true } = {}) {
  root.dataset.theme = theme;
  document.querySelector('meta[name="theme-color"]').setAttribute("content", theme === "dark" ? "#101310" : "#f4f4ef");
  syncThemeButton();
  if (persist) writePreference("theme", theme);
}

function setPageInert(inert) {
  [mainContent, siteFooter].forEach((element) => {
    element.inert = inert;
    element.toggleAttribute("inert", inert);
    if (inert) element.setAttribute("aria-hidden", "true");
    else element.removeAttribute("aria-hidden");
  });
}

function closeMenu({ restoreFocus = true } = {}) {
  if (mobileNav.hidden) return;
  mobileNav.hidden = true;
  header.classList.remove("menu-active");
  document.body.classList.remove("menu-open");
  setPageInert(false);
  syncMenuButton();
  if (restoreFocus && menuReturnFocus instanceof HTMLElement) {
    menuReturnFocus.focus({ preventScroll: true });
  }
  menuReturnFocus = null;
}

function openMenu() {
  menuReturnFocus = document.activeElement;
  mobileNav.hidden = false;
  header.classList.add("menu-active");
  document.body.classList.add("menu-open");
  setPageInert(true);
  syncMenuButton();
  window.requestAnimationFrame(() => mobileNav.querySelector("a")?.focus());
}

function toggleMenu() {
  if (mobileNav.hidden) openMenu();
  else closeMenu();
}

languageButton.addEventListener("click", () => setLanguage(language === "en" ? "zh" : "en"));
themeButton.addEventListener("click", () => setTheme(root.dataset.theme === "dark" ? "light" : "dark"));
menuButton.addEventListener("click", toggleMenu);
mobileNav.querySelectorAll("a").forEach((link) => {
  link.addEventListener("click", () => {
    const target = document.querySelector(link.getAttribute("href"));
    closeMenu({ restoreFocus: false });
    window.setTimeout(() => {
      if (!(target instanceof HTMLElement)) return;
      const hadTabIndex = target.hasAttribute("tabindex");
      target.setAttribute("tabindex", "-1");
      target.focus({ preventScroll: true });
      if (!hadTabIndex) {
        target.addEventListener("blur", () => target.removeAttribute("tabindex"), { once: true });
      }
    }, 0);
  });
});
document.addEventListener("keydown", (event) => {
  if (event.key === "Escape" && !mobileNav.hidden) {
    event.preventDefault();
    closeMenu();
  }
});

function applyPublicationFilter(filter) {
  let count = 0;
  filterButtons.forEach((button) => {
    const selected = button.dataset.filter === filter;
    button.classList.toggle("active", selected);
    button.setAttribute("aria-pressed", String(selected));
  });
  papers.forEach((paper) => {
    const visible = filter === "all" || paper.dataset.category.split(" ").includes(filter);
    paper.hidden = !visible;
    if (visible) count += 1;
    else paper.querySelectorAll("video").forEach((video) => video.pause());
  });
  updateFilterStatus(count);
}

filterButtons.forEach((button) => {
  button.addEventListener("click", () => applyPublicationFilter(button.dataset.filter));
});

document.querySelectorAll(".paper-media").forEach((media, mediaIndex) => {
  const tabs = [...media.querySelectorAll("[data-media-target]")];
  const panels = [...media.querySelectorAll("[data-media-panel]")];
  let loadTimer = 0;

  function panelFor(tab) {
    return panels.find((panel) => panel.dataset.mediaPanel === tab.dataset.mediaTarget);
  }

  function clearLoadTimer() {
    window.clearTimeout(loadTimer);
    loadTimer = 0;
  }

  function prepareVideoPanel(panel) {
    const video = panel.querySelector("video");
    if (!video || panel.querySelector(".media-feedback")) return;
    const source = video.dataset.src;
    const status = document.createElement("span");
    const feedback = document.createElement("div");
    const copy = document.createElement("p");
    const actions = document.createElement("div");
    const retry = document.createElement("button");
    const direct = document.createElement("a");

    status.className = "sr-only";
    status.setAttribute("role", "status");
    status.setAttribute("aria-live", "polite");
    status.dataset.mediaStatus = "";
    feedback.className = "media-feedback";
    copy.dataset.mediaErrorCopy = "";
    actions.className = "media-feedback-actions";
    retry.className = "media-retry";
    retry.type = "button";
    retry.dataset.mediaRetry = "";
    direct.className = "media-open";
    direct.href = source;
    direct.target = "_blank";
    direct.rel = "noreferrer";
    direct.dataset.mediaOpen = "";
    actions.append(retry, direct);
    feedback.append(copy, actions);
    panel.append(status, feedback);

    const finishLoading = () => {
      clearLoadTimer();
      panel.classList.remove("is-loading", "has-error");
      panel.removeAttribute("aria-busy");
      status.textContent = "";
    };
    const failLoading = () => {
      if (video.readyState >= HTMLMediaElement.HAVE_FUTURE_DATA) {
        finishLoading();
        return;
      }
      clearLoadTimer();
      panel.classList.remove("is-loading");
      panel.classList.add("has-error");
      panel.removeAttribute("aria-busy");
      status.textContent = text("common.videoError");
    };
    const loadVideo = () => {
      if (!source || panel.classList.contains("is-loading")) return;
      if (video.readyState >= HTMLMediaElement.HAVE_FUTURE_DATA && video.currentSrc) {
        finishLoading();
        return;
      }
      panel.classList.remove("has-error");
      panel.classList.add("is-loading");
      panel.setAttribute("aria-busy", "true");
      status.textContent = text("common.loadingVideo");
      if (video.dataset.poster && !video.poster) video.poster = video.dataset.poster;
      if (video.getAttribute("src") !== source) video.src = source;
      video.load();
      clearLoadTimer();
      loadTimer = window.setTimeout(failLoading, 15000);
    };

    video.addEventListener("canplay", finishLoading);
    video.addEventListener("error", failLoading);
    video.addEventListener("abort", failLoading);
    video.addEventListener("stalled", () => {
      if (panel.classList.contains("is-loading")) status.textContent = text("common.loadingVideo");
    });
    retry.addEventListener("click", () => {
      video.pause();
      video.removeAttribute("src");
      video.load();
      window.setTimeout(loadVideo, 0);
    });
    panel.loadVideo = loadVideo;
  }

  function activateTab(tab, { focus = false } = {}) {
    const targetPanel = panelFor(tab);
    tabs.forEach((item) => {
      const selected = item === tab;
      item.classList.toggle("active", selected);
      item.setAttribute("aria-selected", String(selected));
      item.tabIndex = selected ? 0 : -1;
    });
    panels.forEach((panel) => {
      const selected = panel === targetPanel;
      panel.hidden = !selected;
      panel.classList.toggle("active", selected);
      if (!selected) panel.querySelectorAll("video").forEach((video) => video.pause());
    });
    if (tab.dataset.mediaTarget === "demo") targetPanel?.loadVideo?.();
    if (focus) tab.focus();
  }

  panels.forEach(prepareVideoPanel);
  tabs.forEach((tab, tabIndex) => {
    const panel = panelFor(tab);
    const tabId = `media-${mediaIndex}-tab-${tab.dataset.mediaTarget}`;
    const panelId = `media-${mediaIndex}-panel-${tab.dataset.mediaTarget}`;
    tab.id = tabId;
    tab.setAttribute("role", "tab");
    tab.setAttribute("aria-controls", panelId);
    tab.tabIndex = tab.classList.contains("active") ? 0 : -1;
    panel.id = panelId;
    panel.setAttribute("role", "tabpanel");
    panel.setAttribute("aria-labelledby", tabId);
    tab.addEventListener("click", () => activateTab(tab));
    tab.addEventListener("keydown", (event) => {
      const keyMap = {
        ArrowLeft: (tabIndex - 1 + tabs.length) % tabs.length,
        ArrowRight: (tabIndex + 1) % tabs.length,
        Home: 0,
        End: tabs.length - 1
      };
      const nextIndex = keyMap[event.key];
      if (nextIndex === undefined) return;
      event.preventDefault();
      activateTab(tabs[nextIndex], { focus: true });
    });
  });
});

function syncHeader() {
  header.classList.toggle("scrolled", window.scrollY > 24);
}

const sectionLinks = [...document.querySelectorAll('.desktop-nav a[href^="#"], .mobile-nav a[href^="#"]')];
const sections = [...new Set(sectionLinks.map((link) => document.querySelector(link.getAttribute("href"))).filter(Boolean))];
if ("IntersectionObserver" in window) {
  const sectionObserver = new IntersectionObserver(
    (entries) => {
      const visible = entries.filter((entry) => entry.isIntersecting).sort((a, b) => b.intersectionRatio - a.intersectionRatio)[0];
      if (!visible) return;
      sectionLinks.forEach((link) => {
        if (link.getAttribute("href") === `#${visible.target.id}`) link.setAttribute("aria-current", "location");
        else link.removeAttribute("aria-current");
      });
    },
    { rootMargin: "-24% 0px -60%", threshold: [0.01, 0.2, 0.45] }
  );
  sections.forEach((section) => sectionObserver.observe(section));
}

window.addEventListener("scroll", syncHeader, { passive: true });
window.addEventListener("resize", () => {
  if (window.innerWidth > 1080 && !mobileNav.hidden) {
    closeMenu({ restoreFocus: false });
    document.querySelector(".wordmark")?.focus({ preventScroll: true });
  }
});
if (!storedTheme) {
  systemThemeQuery.addEventListener?.("change", (event) => {
    if (!readPreference("theme")) setTheme(event.matches ? "dark" : "light", { persist: false });
  });
}

document.getElementById("year").textContent = new Date().getFullYear();

const preferredTheme = systemThemeQuery.matches ? "dark" : "light";
setLanguage(language);
setTheme(storedTheme === "dark" || storedTheme === "light" ? storedTheme : preferredTheme, { persist: false });
applyPublicationFilter("all");
syncMediaLanguage();
syncHeader();
updateIcons();
